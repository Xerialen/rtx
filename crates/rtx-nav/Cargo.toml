[package]
name = "rtx-nav"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
build = "build.rs"

# Shardable, deterministic ground-turn-curl candidate search harness (route-lab
# docs/plans/2026-07-18-vmonster-gt-curl-farm.md). No new dependencies.
[[bin]]
name = "gt-search"
path = "src/bin/gt_search.rs"

[dependencies]
binrw.workspace = true
glam.workspace = true
rayon.workspace = true
serde = { version = "1", features = ["derive"] }
serde_json = "1"
