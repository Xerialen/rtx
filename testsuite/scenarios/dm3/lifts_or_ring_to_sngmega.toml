schema = "rtx-scenario/1"
name = "lifts_or_ring_to_sngmega"
map = "dm3"
kind = "goto"
category = "grunddrill"
place = "lifts → SNG mega"
description = "lifts-or-ring-to-sngmega: anchored on the owner's own run of the route (7.74 s, peak 539 u/s, ending at mh@SNG.MH). The bot has to arrive within 8.67 s to pass."

[run]
# Anchors and reference time come from lifts-or-ring-to-sngmega.qwd via the owner's route
# manifest (see scenarios/dm3/routes-v1.json).
start = [514.4, 759.6, 216.0]
target = [-688.0, 78.1, 184.0]
attempts = 5
timeout_s = 31
pause_s = 1.0
arrive_box = 70
regoto_max = 6

[route]
# Ordered waypoints the attempt must pass through, in this order,
# before an arrival counts. Anchored on the owner's own run of the
# route: points he actually occupied, in the order he occupied
# them, at the places his path genuinely departs from a straight
# line to the target — never points derived from the navmesh or
# from geometry.
via = [
  { at = [483.8, 597.8, 56.0], box = 96, name = "lifts under" },
  { at = [-180.4, 768.0, 132.5], box = 96, name = "SNG hyllan" },
  { at = [-459.0, 499.4, 120.0], box = 96, name = "SNG" },
  { at = [-856.5, 265.5, 207.2], box = 96, name = "SNG mega" },
]

[threshold]
required = 4
reference_time_s = 7.74
max_time_s = 8.67
