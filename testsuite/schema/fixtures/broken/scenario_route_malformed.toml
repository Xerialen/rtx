schema = "rtx-scenario/1"
name = "fixture_goto_route"
map = "dm3"
kind = "goto"
category = "grunddrill"
place = "fixturplats"
description = "Route waypoint missing its box, so the gate cannot be tested."

[run]
start = [0, 0, 0]
target = [32, 0, 0]
attempts = 10
timeout_s = 5
pause_s = 0.5
arrive_box = 32
regoto_max = 2

[threshold]
required = 10

[route]
via = [
  { at = [10.0, 0.0, 0.0], name = "window" },
]
